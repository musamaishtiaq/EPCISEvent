@model EPCISEvent.MasterData.MainClasses.EventData

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>EventData</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EventType" class="control-label"></label>
                <select asp-for="EventType" asp-items="@EPCISEvent.Controllers.EventDatasController.GetBusinessEventTypeSelectList()" class="form-control">
                    <option value="">-- Select Event Type --</option>
                </select>
                <span asp-validation-for="EventType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
